<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jinjiang.ssm.mapper.UserMapper">
    <update id="update">
        UPDATE  user SET USER_NAME='allen' WHERE  id='${id}'
    </update>


    <select id="login" resultType="com.jinjiang.ssm.domain.User">
        SELECT * FROM  user WHERE  id = '${id}'
    </select>

</mapper>